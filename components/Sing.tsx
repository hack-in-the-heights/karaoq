import * as React from 'react';

import styles from '../styles/Sing.module.css';

const Sing = (): React.ReactElement => {
  return (
    <main className={styles.main}>
      <h1 className={styles.title}>Please sing</h1>
    </main>
  );
};

export default Sing;
